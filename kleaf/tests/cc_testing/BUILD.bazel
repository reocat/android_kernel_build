load("@bazel_skylib//rules:build_test.bzl", "build_test")

cc_binary(
    name = "android_arm64_bin",
    srcs = ["target.c"],
    target_compatible_with = [
        "@platforms//os:android",
        "@platforms//cpu:arm64",
    ],
)

cc_binary(
    name = "android_x86_64_bin",
    srcs = ["target.c"],
    target_compatible_with = [
        "@platforms//os:android",
        "@platforms//cpu:x86_64",
    ],
)

cc_binary(
    name = "linux_x86_64_bin",
    srcs = ["host.c"],
    target_compatible_with = [
        "@platforms//os:linux",
        "@platforms//cpu:x86_64",
    ],
)
